//
//  MessageView.swift
//  JKVncServer
//
//  Created by 板垣勇次 on 2018/07/05.
//  Copyright © 2018年 JVCKENWOOD Engineering Corporation. All rights reserved.
//

import UIKit

/// メッセージ表示用ラベルクラス
class MessageLabel: UILabel {
    /*
     /// メッセージ用ビューを表示する
     func show() {
     // 非表示状態とき
     if !isHidden {
     // ビューを表示する
     UIView.animate(withDuration: 0.5, animations: {
     self.alpha = 1.0
     }) { _ in
     self.isHidden = false
     }
     }
     }
     
     /// メッセージ用ビューを非表示にする
     func hide() {
     // 表示状態とき
     if isHidden {
     // ビューを非表示にする
     UIView.animate(withDuration: 0.5, animations: {
     self.alpha = 0
     }) { _ in
     self.isHidden = true
     }
     }
     }
     */
}
